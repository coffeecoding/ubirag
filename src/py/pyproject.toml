[project]
name = "ubirag-py"
version = "0.1.0"
description = "UbiRAG Python services workspace"
requires-python = ">=3.12"

[tool.uv.workspace]
members = ["template-service"]

[dependency-groups]
dev = []
